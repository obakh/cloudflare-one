---
title: Authentication
description: Learn how to implement authentication
---

# Authentication

This guide covers how authentication works in the project.

## Overview

The project uses [Better Auth](https://better-auth.com) for authentication, supporting multiple providers:

- Email/Password
- Google OAuth
- GitHub OAuth
- Apple OAuth

## Configuration

### Environment Variables

Add these to your `.env` file:

```bash
# Auth
BETTER_AUTH_SECRET=your-secret-key
BETTER_AUTH_URL=http://localhost:5174

# OAuth Providers (optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
```

## Usage

### Sign In

```typescript
import { authClient } from "@repo/auth/client";

// Email sign in
await authClient.signIn.email({
  email: "user@example.com",
  password: "password123",
});

// OAuth sign in
await authClient.signIn.social({
  provider: "google",
});
```

### Sign Out

```typescript
await authClient.signOut();
```

### Get Current User

```typescript
const { data: session } = await authClient.getSession();
const user = session?.user;
```

## Protected Routes

Use the auth middleware to protect routes:

```typescript
import { auth } from "@repo/auth";

export async function loader({ request }) {
  const session = await auth.api.getSession({
    headers: request.headers,
  });
  
  if (!session) {
    throw redirect("/sign-in");
  }
  
  return { user: session.user };
}
```
